{
  "name": "shift-manager",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "mock-data": "node scripts/write-mock-data.mjs",
    "test": "node --test test/**/*.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
