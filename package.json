{
  "name": "shift-manager",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "mock-data": "node scripts/write-mock-data.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
